# Fonts Directory

请在此目录放置示例或项目使用的 TrueType / OpenType 字体文件。  
示例程序 `44_text_render_test` 默认尝试加载 `Roboto-Regular.ttf`，你可以：

1. 将 [Google Fonts](https://fonts.google.com/specimen/Roboto) 的 `Roboto-Regular.ttf` 复制到此目录；或  
2. 修改示例代码中的 `kFontPath` 以指向自己的字体资源。

字体文件通常受许可证约束，请确保遵守对应授权条款。


