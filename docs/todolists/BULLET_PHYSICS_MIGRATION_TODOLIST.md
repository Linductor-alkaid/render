# Bullet Physics 迁移计划 Todolist

> **版本**: v1.0  
> **日期**: 2025-12-10  
> **基于**: `BULLET_PHYSICS_MIGRATION.md`  
> **目标**: 将自定义物理引擎替换为 Bullet Physics，保持 API 向后兼容

---

## 目录

1. [阶段一：基础适配层](#阶段一基础适配层)
2. [阶段二：世界适配器](#阶段二世界适配器)
3. [阶段三：PhysicsWorld 集成](#阶段三physicsworld-集成)
4. [阶段四：关节约束](#阶段四关节约束)
5. [阶段五：力场系统](#阶段五力场系统)
6. [阶段六：测试和优化](#阶段六测试和优化)
7. [阶段七：文档和清理](#阶段七文档和清理)

---

## 阶段一：基础适配层

**优先级**: 🔴 Critical  
**预计时间**: 1-2 周  
**依赖**: 无

### 1.1 项目结构搭建

- [x] **1.1.1** 创建适配层目录结构
  - [x] 创建 `src/physics/bullet_adapter/` 目录
  - [x] 创建 `include/render/physics/bullet_adapter/` 目录
  - [x] 创建基础文件结构：
    ```
    src/physics/bullet_adapter/
    ├── eigen_to_bullet.cpp
    ├── bullet_world_adapter.cpp
    ├── bullet_rigid_body_adapter.cpp
    └── bullet_shape_adapter.cpp
    
    include/render/physics/bullet_adapter/
    ├── eigen_to_bullet.h
    ├── bullet_world_adapter.h
    ├── bullet_rigid_body_adapter.h
    └── bullet_shape_adapter.h
    ```

- [x] **1.1.2** 配置 CMake 集成
  - [x] 在 `CMakeLists.txt` 中添加 `USE_BULLET_PHYSICS` 选项
  - [x] 配置 Bullet 子项目（`third_party/bullet3`）
  - [x] 设置 Bullet 编译选项（禁用 demos、tests 等）
  - [x] 链接 Bullet 库（BulletDynamics, BulletCollision, LinearMath）
  - [x] 添加包含目录

**验证标准**:
- ✅ 项目可以编译（即使适配层为空）
- ✅ Bullet 库正确链接
- ✅ 可以通过 `-DUSE_BULLET_PHYSICS=ON` 启用 Bullet

---

### 1.2 数据转换层

- [x] **1.2.1** 实现 Vector3 转换
  - [x] `ToBullet(const Vector3&)` → `btVector3`
  - [x] `FromBullet(const btVector3&)` → `Vector3`
  - [x] 编写单元测试验证转换正确性

- [x] **1.2.2** 实现 Quaternion 转换
  - [x] 注意四元数顺序差异（Eigen: w,x,y,z vs Bullet: x,y,z,w）
  - [x] `ToBullet(const Quaternion&)` → `btQuaternion`
  - [x] `FromBullet(const btQuaternion&)` → `Quaternion`
  - [x] 编写单元测试验证转换正确性（包括旋转一致性）

- [x] **1.2.3** 实现 Matrix3 转换
  - [x] `ToBullet(const Matrix3&)` → `btMatrix3x3`
  - [x] `FromBullet(const btMatrix3x3&)` → `Matrix3`
  - [x] 编写单元测试验证转换正确性

- [x] **1.2.4** 实现 Transform 转换
  - [x] `ToBullet(const Vector3& pos, const Quaternion& rot)` → `btTransform`
  - [x] `FromBullet(const btTransform&, Vector3& pos, Quaternion& rot)`
  - [x] 编写单元测试验证转换正确性

**验证标准**:
- ✅ 所有转换函数通过单元测试
- ✅ 四元数转换后旋转保持一致
- ✅ 矩阵转换后数学运算结果一致

---

### 1.3 形状适配器

- [x] **1.3.1** 实现基础形状创建
  - [x] `BulletShapeAdapter::CreateShape(const ColliderComponent&)`
  - [x] 支持 `ShapeType::Sphere` → `btSphereShape`
  - [x] 支持 `ShapeType::Box` → `btBoxShape`
  - [x] 支持 `ShapeType::Capsule` → `btCapsuleShape`
  - [x] 编写单元测试

- [x] **1.3.2** 实现复杂形状创建
  - [x] 支持 `ShapeType::ConvexHull` → `btConvexHullShape`
  - [x] 支持 `ShapeType::Mesh` → `btBvhTriangleMeshShape`
  - [x] 处理网格数据转换（`Mesh` → Bullet 网格）
  - [x] 编写单元测试

- [x] **1.3.3** 实现形状更新逻辑
  - [x] `UpdateShape(btCollisionShape*, const ColliderComponent&)`
  - [x] 处理形状参数动态修改
  - [x] 处理形状类型切换

- [x] **1.3.4** 实现形状局部变换
  - [x] 处理 `ColliderComponent::center` 偏移
  - [x] 处理 `ColliderComponent::rotation` 旋转
  - [x] 使用 `btTransform` 存储局部变换

- [x] **1.3.5** 实现形状共享优化
  - [x] 创建形状缓存机制
  - [x] 相同参数的形状只创建一次
  - [x] 实现形状引用计数管理

**验证标准**:
- ✅ 所有形状类型正确创建
- ✅ 形状参数正确映射
- ✅ 局部变换正确应用
- ✅ 形状共享减少内存占用

---

### 1.4 刚体适配器

- [x] **1.4.1** 实现基础同步接口
  - [x] `BulletRigidBodyAdapter::SyncToBullet(const RigidBodyComponent&)`
  - [x] `BulletRigidBodyAdapter::SyncFromBullet(RigidBodyComponent&)`
  - [x] 处理基本属性（质量、速度、力、扭矩）

- [x] **1.4.2** 实现刚体类型转换
  - [x] `BodyType::Static` → `CF_STATIC_OBJECT` + 质量 = 0
  - [x] `BodyType::Kinematic` → `CF_KINEMATIC_OBJECT` + 质量 = 0
  - [x] `BodyType::Dynamic` → 正常质量
  - [x] 处理类型切换

- [x] **1.4.3** 实现质量属性同步
  - [x] 同步 `mass` 和 `inverseMass`
  - [x] 同步 `inertiaTensor` 和 `inverseInertiaTensor`
  - [x] 使用 `btRigidBody::setMassProps()`

- [x] **1.4.4** 实现速度约束同步
  - [x] 处理 `lockPosition[3]` → `setLinearFactor()`
  - [x] 处理 `lockRotation[3]` → `setAngularFactor()`
  - [x] 处理 `maxLinearSpeed` 和 `maxAngularSpeed`

- [x] **1.4.5** 实现阻尼同步
  - [x] 同步 `linearDamping` → `setDamping()`
  - [x] 同步 `angularDamping` → `setDamping()`

- [x] **1.4.6** 实现重力同步
  - [x] 处理 `useGravity` → `setGravity()`（通过设置重力向量为零来禁用）
  - [x] 实现 `gravityScale`（在应用重力时乘以缩放因子，需在世界适配器中处理）

- [x] **1.4.7** 实现 CCD 同步
  - [x] 处理 `useCCD` → `setCcdMotionThreshold()`
  - [x] 处理 `ccdVelocityThreshold` 自动判断
  - [x] 设置 `setCcdSweptSphereRadius()`

- [x] **1.4.8** 实现休眠状态同步
  - [x] 同步 `isSleeping` ↔ `getActivationState()`
  - [x] 处理休眠阈值 `sleepThreshold` → `setSleepingThresholds()`

**验证标准**:
- ✅ 所有刚体属性正确同步
- ✅ 刚体类型转换正确
- ✅ 速度约束正确应用
- ✅ 重力缩放正确工作

---

## 阶段二：世界适配器

**优先级**: 🔴 Critical  
**预计时间**: 2-3 周  
**依赖**: 阶段一完成

### 2.1 BulletWorldAdapter 核心功能

- [x] **2.1.1** 实现世界初始化
  - [x] 创建 `btBroadphaseInterface`（使用 `btDbvtBroadphase`）
  - [x] 创建 `btCollisionDispatcher`
  - [x] 创建 `btConstraintSolver`（使用 `btSequentialImpulseConstraintSolver`）
  - [x] 创建 `btCollisionConfiguration`（使用 `btDefaultCollisionConfiguration`）
  - [x] 创建 `btDiscreteDynamicsWorld`

- [x] **2.1.2** 实现世界配置同步
  - [x] 同步 `PhysicsConfig::gravity` → `setGravity()`
  - [x] 同步 `PhysicsConfig::solverIterations` → `getSolverInfo().m_numIterations`
  - [x] 同步 `PhysicsConfig::positionIterations`
  - [x] 同步 `PhysicsConfig::enableCCD` → `getDispatchInfo().m_useContinuous`
  - [x] 同步 `PhysicsConfig::enableSleeping`

- [x] **2.1.3** 实现 Step() 方法
  - [x] 调用 `btDiscreteDynamicsWorld::stepSimulation(deltaTime)`
  - [x] 处理固定时间步长（`fixedDeltaTime`）
  - [x] 处理最大子步数（`maxSubSteps`）

- [x] **2.1.4** 实现实体到刚体映射
  - [x] 维护 `std::unordered_map<ECS::EntityID, btRigidBody*>`
  - [x] 维护 `std::unordered_map<btRigidBody*, ECS::EntityID>`
  - [x] 实现添加/移除映射的方法

**验证标准**:
- ✅ 物理世界正确初始化
- ✅ 配置参数正确应用
- ✅ Step() 方法正确执行物理更新

---

### 2.2 实体管理

- [x] **2.2.1** 实现刚体添加
  - [x] `AddRigidBody(ECS::EntityID, const RigidBodyComponent&, const ColliderComponent&)`
  - [x] 创建 `btRigidBody`
  - [x] 创建并附加 `btCollisionShape`
  - [x] 添加到 `btDiscreteDynamicsWorld`
  - [x] 更新实体映射

- [x] **2.2.2** 实现刚体移除
  - [x] `RemoveRigidBody(ECS::EntityID)`
  - [x] 从 `btDiscreteDynamicsWorld` 移除
  - [x] 清理实体映射
  - [x] 释放 Bullet 对象（注意形状共享）

- [x] **2.2.3** 实现刚体更新检测
  - [x] 检测组件变化（质量、形状、类型等）
  - [x] 自动更新对应的 Bullet 对象
  - [x] 处理形状替换

**验证标准**:
- ✅ 刚体正确添加到物理世界
- ✅ 刚体正确从物理世界移除
- ✅ 组件变化正确同步到 Bullet

---

### 2.3 碰撞检测集成

- [x] **2.3.1** 实现碰撞层和掩码过滤
  - [x] 同步 `ColliderComponent::collisionLayer` → `m_collisionFilterGroup`
  - [x] 同步 `ColliderComponent::collisionMask` → `m_collisionFilterMask`
  - [x] 实现自定义碰撞过滤回调（在 `BulletContactCallback::needsCollision()` 中）

- [x] **2.3.2** 实现触发器检测
  - [x] 处理 `ColliderComponent::isTrigger` → `CF_NO_CONTACT_RESPONSE`
  - [x] 实现触发器事件回调（基础实现完成，触发器判断在 PhysicsWorld 中根据 ColliderComponent 处理）
  - [x] 同步触发器状态到 ECS（通过事件系统）

- [x] **2.3.3** 实现碰撞事件回调
  - [x] 创建 `BulletContactCallback` 类
  - [x] 实现 `addSingleResult()` 方法
  - [x] 收集碰撞信息（接触点、法线、深度）
  - [x] 触发 ECS 碰撞事件（在 `BulletWorldAdapter::SendCollisionEvents()` 中）

- [x] **2.3.4** 实现碰撞结果同步
  - [x] 同步碰撞信息到 `ColliderComponent`（通过事件系统）
  - [x] 触发碰撞事件（CollisionEnterEvent, CollisionStayEvent, CollisionExitEvent）

**验证标准**:
- ✅ 碰撞层过滤正确工作
- ✅ 触发器正确检测但不产生物理响应
- ✅ 碰撞事件正确触发

---

### 2.4 物理材质处理

- [x] **2.4.1** 实现摩擦系数同步
  - [x] 同步 `PhysicsMaterial::friction` → `setFriction()`
  - [x] 处理材质变化（在 `AddRigidBody` 和 `UpdateRigidBody` 中）

- [x] **2.4.2** 实现弹性系数同步
  - [x] 同步 `PhysicsMaterial::restitution` → `setRestitution()`
  - [x] 处理材质变化（在 `AddRigidBody` 和 `UpdateRigidBody` 中）

- [x] **2.4.3** 实现材质组合模式
  - [x] 实现自定义接触回调 `BulletMaterialCallback`
  - [x] 处理 `frictionCombine`（Average/Minimum/Maximum/Multiply）
  - [x] 处理 `restitutionCombine`（Average/Minimum/Maximum/Multiply）
  - [x] 在 `ProcessContactPoint()` 中应用组合逻辑（通过 Bullet 的 `gContactProcessedCallback` 全局回调）

**验证标准**:
- ✅ 摩擦系数正确应用
- ✅ 弹性系数正确应用
- ✅ 材质组合模式正确工作

---

## 阶段三：PhysicsWorld 集成

**优先级**: 🔴 Critical  
**预计时间**: 1-2 周  
**依赖**: 阶段二完成

### 3.1 条件编译支持

- [x] **3.1.1** 修改 PhysicsWorld 头文件
  - [x] 添加 `#ifdef USE_BULLET_PHYSICS` 条件编译
  - [x] 添加 `BulletWorldAdapter` 成员变量
  - [x] 保持原有实现可用（向后兼容）

- [x] **3.1.2** 修改 PhysicsWorld 实现
  - [x] 在构造函数中根据条件编译选择后端
  - [x] 保持原有实现不变（通过 `StepLegacy()` 方法）
  - [x] 实现 `SyncECSToBullet()` 和 `SyncBulletToECS()` 方法

**验证标准**:
- ✅ 可以通过编译选项切换后端
- ✅ 原有代码无需修改即可编译

---

### 3.2 同步机制

- [x] **3.2.1** 实现 ECS → Bullet 同步
  - [x] 在 `Step()` 开始时同步所有组件
  - [x] 同步 `TransformComponent` → `btRigidBody`（Kinematic/Static）
  - [x] 同步 `RigidBodyComponent` → `btRigidBody`
  - [x] 同步 `ColliderComponent` → `btCollisionShape`
  - [x] 处理新添加的实体

- [x] **3.2.2** 实现 Bullet → ECS 同步
  - [x] 在 `Step()` 结束后同步所有结果
  - [x] 同步 `btRigidBody` → `TransformComponent`（Dynamic）
  - [x] 同步 `btRigidBody` → `RigidBodyComponent`
  - [x] 处理移除的实体（基础实现完成，实体移除应通过显式调用或ECS事件处理）

- [x] **3.2.3** 实现 Kinematic 物体驱动
  - [x] 从 `TransformComponent` 读取位置/旋转
  - [x] 同步到 `btRigidBody`
  - [x] 确保 Kinematic 物体不受力影响（通过 Bullet 的 Kinematic 标志）

- [x] **3.2.4** 实现插值变换
  - [x] 保存上一帧的变换（`previousPosition`, `previousRotation`）
  - [x] 实现 `InterpolateTransforms(float alpha)`（使用 PhysicsTransformSync）
  - [x] 在渲染前调用插值

**验证标准**:
- ✅ 物理更新正确同步到渲染
- ✅ Kinematic 物体正确驱动
- ✅ 插值变换平滑

---

### 3.3 PhysicsWorld::Step() 实现

- [ ] **3.3.1** 实现 Bullet 版本的 Step()
  - [ ] 1. 同步 ECS → Bullet（所有组件）
  - [ ] 2. 应用力场（如果有）
  - [ ] 3. 调用 `BulletWorldAdapter::Step()`
  - [ ] 4. 同步 Bullet → ECS（所有结果）
  - [ ] 5. 插值变换

- [ ] **3.3.2** 处理固定时间步长
  - [ ] 实现子步长逻辑
  - [ ] 处理 `maxSubSteps` 限制
  - [ ] 处理时间累积

**验证标准**:
- ✅ Step() 方法正确执行完整物理更新流程
- ✅ 固定时间步长正确工作

---

## 阶段四：关节约束

**优先级**: 🟡 High  
**预计时间**: 2-3 周  
**依赖**: 阶段三完成

### 4.1 关节适配器基础

- [ ] **4.1.1** 创建关节适配器基类
  - [ ] 定义 `BulletJointAdapter` 基类
  - [ ] 实现通用接口（同步、更新、断裂检测）

- [ ] **4.1.2** 实现关节管理
  - [ ] 维护关节到 `btTypedConstraint` 的映射
  - [ ] 实现关节添加/移除

**验证标准**:
- ✅ 关节适配器框架正确搭建

---

### 4.2 固定关节

- [ ] **4.2.1** 实现固定关节适配器
  - [ ] `FixedJointAdapter` 类
  - [ ] 使用 `btFixedConstraint`
  - [ ] 同步 `FixedJointData` 属性

- [ ] **4.2.2** 实现固定关节同步
  - [ ] 同步锚点（`localAnchorA`, `localAnchorB`）
  - [ ] 同步相对位置和旋转

**验证标准**:
- ✅ 固定关节正确约束两个刚体
- ✅ 相对位置和旋转保持不变

---

### 4.3 铰链关节

- [ ] **4.3.1** 实现铰链关节适配器
  - [ ] `HingeJointAdapter` 类
  - [ ] 使用 `btHingeConstraint`
  - [ ] 同步 `HingeJointData` 属性

- [ ] **4.3.2** 实现角度限制
  - [ ] 同步 `hasLimits`, `limitMin`, `limitMax`
  - [ ] 使用 `setLimit()`

- [ ] **4.3.3** 实现铰链马达
  - [ ] 同步 `useMotor`, `motorSpeed`, `motorMaxForce`
  - [ ] 使用 `enableMotor()`, `setMotorTargetVelocity()`, `setMaxMotorImpulse()`

- [ ] **4.3.4** 实现旋转轴同步
  - [ ] 同步 `localAxisA`, `localAxisB`

**验证标准**:
- ✅ 铰链关节正确约束旋转
- ✅ 角度限制正确工作
- ✅ 马达正确驱动旋转

---

### 4.4 距离关节

- [ ] **4.4.1** 实现距离关节适配器
  - [ ] `DistanceJointAdapter` 类
  - [ ] 使用 `btPoint2PointConstraint` 或 `btGeneric6DofSpringConstraint`
  - [ ] 同步 `DistanceJointData` 属性

- [ ] **4.4.2** 实现距离限制
  - [ ] 同步 `restLength`
  - [ ] 同步 `hasLimits`, `minDistance`, `maxDistance`

**验证标准**:
- ✅ 距离关节维持固定距离
- ✅ 距离限制正确工作

---

### 4.5 弹簧关节

- [ ] **4.5.1** 实现弹簧关节适配器
  - [ ] `SpringJointAdapter` 类
  - [ ] 使用 `btGeneric6DofSpringConstraint`
  - [ ] 同步 `SpringJointData` 属性

- [ ] **4.5.2** 实现弹簧参数
  - [ ] 同步 `restLength` → `setEquilibriumPoint()`
  - [ ] 同步 `stiffness` → `setStiffness()`
  - [ ] 同步 `damping` → `setDamping()`

**验证标准**:
- ✅ 弹簧关节产生弹性效果
- ✅ 弹簧参数正确应用

---

### 4.6 滑动关节

- [ ] **4.6.1** 实现滑动关节适配器
  - [ ] `SliderJointAdapter` 类
  - [ ] 使用 `btSliderConstraint`
  - [ ] 同步 `SliderJointData` 属性

- [ ] **4.6.2** 实现滑动限制
  - [ ] 同步 `localAxis`
  - [ ] 同步 `hasLimits`, `minDistance`, `maxDistance`
  - [ ] 使用 `setLowerLinLimit()`, `setUpperLinLimit()`

**验证标准**:
- ✅ 滑动关节正确约束滑动
- ✅ 滑动限制正确工作

---

### 4.7 关节特性

- [ ] **4.7.1** 实现关节断裂检测
  - [ ] 检测关节上的力和扭矩
  - [ ] 比较 `breakForce` 和 `breakTorque`
  - [ ] 超过阈值时标记为断裂
  - [ ] 从物理世界移除断裂的关节

- [ ] **4.7.2** 实现关节启用/禁用
  - [ ] 同步 `isEnabled` → `setEnabled()`

- [ ] **4.7.3** 实现关节碰撞控制
  - [ ] 同步 `enableCollision`
  - [ ] 控制连接的两个刚体是否参与碰撞

**验证标准**:
- ✅ 关节断裂检测正确工作
- ✅ 关节启用/禁用正确工作
- ✅ 关节碰撞控制正确工作

---

## 阶段五：力场系统

**优先级**: 🟡 High  
**预计时间**: 1-2 周  
**依赖**: 阶段三完成

### 5.1 力场系统实现

- [ ] **5.1.1** 创建力场系统类
  - [ ] `BulletForceFieldSystem` 类
  - [ ] 实现力场应用逻辑

- [ ] **5.1.2** 实现重力场/风场
  - [ ] 处理 `ForceFieldComponent::Type::Gravity`
  - [ ] 处理 `ForceFieldComponent::Type::Wind`
  - [ ] 计算方向力：`force = direction * strength * mass * falloff`
  - [ ] 使用 `btRigidBody::applyForce()`

- [ ] **5.1.3** 实现径向力场
  - [ ] 处理 `ForceFieldComponent::Type::Radial`
  - [ ] 计算径向力方向（从力场中心指向物体）
  - [ ] 处理吸引（strength < 0）和排斥（strength > 0）

- [ ] **5.1.4** 实现涡流场
  - [ ] 处理 `ForceFieldComponent::Type::Vortex`
  - [ ] 计算切线方向（垂直于径向和旋转轴）
  - [ ] 应用旋转力

- [ ] **5.1.5** 实现距离检测和衰减
  - [ ] 计算物体到力场中心的距离
  - [ ] 实现线性衰减：`falloff = 1 - (distance / radius)`
  - [ ] 实现平方反比衰减：`falloff = 1 / (1 + distance²)`
  - [ ] 处理 `affectOnlyInside` 标志
  - [ ] 应用 `minFalloff` 限制

- [ ] **5.1.6** 实现层级过滤
  - [ ] 检查 `affectLayers` 掩码
  - [ ] 仅影响匹配的碰撞层

- [ ] **5.1.7** 集成到 PhysicsWorld
  - [ ] 在 `Step()` 中调用力场系统
  - [ ] 在物理更新前应用力场

**验证标准**:
- ✅ 所有力场类型正确工作
- ✅ 距离衰减正确计算
- ✅ 层级过滤正确工作
- ✅ 力场性能可接受

---

## 阶段六：测试和优化

**优先级**: 🔴 Critical  
**预计时间**: 2-3 周  
**依赖**: 阶段一至五完成

### 6.1 单元测试

- [ ] **6.1.1** 数据转换测试
  - [ ] Vector3 转换测试
  - [ ] Quaternion 转换测试（包括旋转一致性）
  - [ ] Matrix3 转换测试
  - [ ] Transform 转换测试

- [ ] **6.1.2** 形状创建测试
  - [ ] 所有形状类型创建测试
  - [ ] 形状参数正确性测试
  - [ ] 形状局部变换测试

- [ ] **6.1.3** 刚体同步测试
  - [ ] 属性同步测试
  - [ ] 类型转换测试
  - [ ] 速度约束测试
  - [ ] 重力缩放测试

- [ ] **6.1.4** 关节功能测试
  - [ ] 所有关节类型测试
  - [ ] 关节限制测试
  - [ ] 关节马达测试
  - [ ] 关节断裂测试

**验证标准**:
- ✅ 所有单元测试通过
- ✅ 测试覆盖率 > 80%

---

### 6.2 集成测试

- [ ] **6.2.1** 物理行为一致性测试
  - [ ] 运行现有物理测试套件
  - [ ] 对比自定义引擎和 Bullet 后端的结果
  - [ ] 允许小的数值误差（< 1%）
  - [ ] 确保视觉上无明显差异

- [ ] **6.2.2** 回归测试
  - [ ] 运行所有现有测试
  - [ ] 确保没有破坏性变更
  - [ ] 修复发现的回归问题

- [ ] **6.2.3** 性能基准测试
  - [ ] 对比两个后端的性能
  - [ ] 测量物理更新时间
  - [ ] 测量内存使用
  - [ ] 确保性能不低于原有实现（理想情况下提升）

**验证标准**:
- ✅ 所有现有测试通过
- ✅ 物理行为视觉上一致
- ✅ 性能不低于原有实现

---

### 6.3 性能优化

- [ ] **6.3.1** 形状共享优化
  - [ ] 实现形状缓存
  - [ ] 减少重复形状创建
  - [ ] 测量内存节省

- [ ] **6.3.2** 同步操作批处理
  - [ ] 减少逐个同步的开销
  - [ ] 批量更新 Bullet 对象
  - [ ] 只在数据变化时同步

- [ ] **6.3.3** 内存池管理
  - [ ] 重用 Bullet 对象
  - [ ] 减少分配/释放开销

- [ ] **6.3.4** 多线程优化（如果适用）
  - [ ] 利用 Bullet 的多线程特性
  - [ ] 配置工作线程数

**验证标准**:
- ✅ 性能提升或至少不下降
- ✅ 内存使用合理

---

## 阶段七：文档和清理

**优先级**: 🟢 Medium  
**预计时间**: 1 周  
**依赖**: 阶段六完成

### 7.1 代码清理

- [ ] **7.1.1** 代码审查
  - [ ] 检查代码风格一致性
  - [ ] 移除未使用的代码
  - [ ] 修复编译警告

- [ ] **7.1.2** 注释和文档
  - [ ] 添加必要的代码注释
  - [ ] 更新 API 文档（如果需要）
  - [ ] 更新迁移文档

**验证标准**:
- ✅ 代码编译无警告
- ✅ 代码风格一致
- ✅ 文档完整

---

### 7.2 迁移指南

- [ ] **7.2.1** 编写迁移指南
  - [ ] 说明如何启用 Bullet 后端
  - [ ] 说明已知差异和注意事项
  - [ ] 提供故障排除指南

- [ ] **7.2.2** 更新项目文档
  - [ ] 更新 README（如果需要）
  - [ ] 更新构建说明

**验证标准**:
- ✅ 迁移指南清晰完整
- ✅ 用户可以顺利迁移

---

## 总结

### 完成标准

迁移完成的标准：

- ✅ 所有阶段任务完成
- ✅ 所有测试通过
- ✅ 物理行为视觉上一致
- ✅ 性能不低于原有实现
- ✅ 代码编译无警告
- ✅ 文档完整

### 时间估算

- **阶段一**：1-2 周
- **阶段二**：2-3 周
- **阶段三**：1-2 周
- **阶段四**：2-3 周
- **阶段五**：1-2 周
- **阶段六**：2-3 周
- **阶段七**：1 周

**总计**：10-16 周（约 2.5-4 个月）

### 风险提示

1. **性能风险**：Bullet 可能在某些场景下性能不如自定义引擎，需要优化
2. **行为差异**：物理行为可能略有不同，需要调整参数
3. **兼容性风险**：某些边缘情况可能不兼容，需要特殊处理

---

**文档版本**: v1.0  
**最后更新**: 2025-12-10  
**维护者**: Linductor

