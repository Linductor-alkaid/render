# 测试程序
# 将来在这里添加单元测试

# 如果使用 Google Test，取消下面的注释：
# find_package(GTest REQUIRED)
# 
# add_executable(render_tests test_main.cpp)
# target_link_libraries(render_tests RenderEngine GTest::GTest GTest::Main)
# 
# add_test(NAME render_tests COMMAND render_tests)

